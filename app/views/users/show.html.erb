<div class="text-center">
    <h1><%= current_user.name %>のマイページ</h1>

    <% current_user.tasks.each do |task| %>
        <%= link_to edit_task_path do %>
            タスク：<%= task.content %>
            タスクの状態：<%= task.status %><br>
        <% end %>
    <% end %>
</div>

<%= link_to "新規タスク作成", new_task_path %>
